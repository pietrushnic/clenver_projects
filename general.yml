apt:
  - tmux
gem:
  - tmuxinator
  - earthquake
https://github.com/pietrushnic/dotfiles.git:
  links:
    gitconfig:
      - $HOME/.gitconfig
    pythonrc:
      - $HOME/.pythonrc
    tmux.conf:
      - $HOME/.tmux.conf
    urlview:
      - $HOME/.urlview

